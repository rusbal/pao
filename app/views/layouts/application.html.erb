<!DOCTYPE html>
<html>
<head>
  <title>Pao</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
  <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
  <h5 class="my-0 mr-md-auto font-weight-normal">Public Attorney's Office <%= current_role %></h5>
    <nav class="my-2 my-md-0 mr-md-3">
      <% if signed_in? %>
        <%= link_to 'Officers', officers_path %>
        <%= link_to 'Waiting', open_requests_path %>
        <%= link_to 'Attended', scheduled_requests_path %>
        <%= link_to 'Done', close_requests_path %>
        <%= link_to 'Lawyers', lawyers_path %>
        <%= link_to 'Admins', admins_path %>
      <% end %>
    </nav>
    <nav class="my-2 my-md-0 mr-md-3">
      <% if signed_in? %>
        <%= current_user.email %>
        <%= button_to 'Sign out', sign_out_path, method: :delete %>
      <% else %>
        <%= link_to 'Sign in', sign_in_path %>
      <% end %>
    </nav>
  </div>
  <div class="container">
    <div class="row">
      <div class="col">
        <% flash.each do |key, value| %>
          <div class="alert alert-<%= notice(key) %> alert-dismissible" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            <%= value %>
           </div>
        <% end %>
        <%= yield %>
      </div>
    </div>
  </div>
</body>
</html>
